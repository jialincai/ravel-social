import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

export default function InboxScreen() {
  return (
    <ThemedView>
      <ThemedText>This is the HomeScreen</ThemedText>
    </ThemedView>
  );
}